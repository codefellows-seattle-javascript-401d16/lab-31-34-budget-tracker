import React from 'react';
import {mount} from 'enzyme';
import ExpenseItem from './index.js';

describe('Testing expense-item', () => {
  test('should be true', () => {
    expect(true).toBe(true);
  });
});
