import React from 'react';
import {shallow} from 'enzyme';
import CategoryItem from './index.js';
import createAppStore from '../../lib/store.js';

describe('Testing Category-Item ', () => {
  test('should return true', () => {
    expect(true).toBe(true);
  });
});
