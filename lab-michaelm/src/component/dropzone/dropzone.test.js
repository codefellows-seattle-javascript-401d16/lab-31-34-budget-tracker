import React from 'react';
import {mount} from 'enzyme';
import Dropzone from './index.js';

describe('Testing dropzone', () => {
  test('should be true', () => {
    expect(true).toBe(true);
  });
});
